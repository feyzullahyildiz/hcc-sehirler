{
  "name": "hcc-sehirler",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "start": "parcel index.html --open",
    "parcel": "parcel",
    "build:gh-pages": "parcel build index.html --public-url /hcc-sehirler",
    "deploy:dist": "gh-pages -d dist",
    "deploy": "npm-run-all build:gh-pages deploy:dist"
  },
  "devDependencies": {
    "gh-pages": "^2.0.1",
    "npm-run-all": "^4.1.5",
    "parcel-bundler": "^1.11.0",
    "sass": "^1.15.2"
  }
}
